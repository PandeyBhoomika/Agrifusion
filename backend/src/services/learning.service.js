/**
 * =====================================================
 * LEARNING SERVICE
 * =====================================================
 */

export const refreshRecommendations = async (
  userId
) => {

  console.log(
    "Refreshing learning recommendations..."
  );

  return {
    success: true,
  };

};